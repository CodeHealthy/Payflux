spring.datasource.url=jdbc:h2:mem:authservice-test-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

spring.flyway.enabled=true

spring.kafka.bootstrap-servers=localhost:9092

spring.data.redis.host=localhost
spring.data.redis.port=6379

spring.mail.host=localhost
spring.mail.port=1025

app.jwt.issuer=payflux-authservice-test
app.jwt.access-token-expiration-minutes=30
app.jwt.refresh-token-expiration-days=7
app.jwt.secret=local-test-secret-change-before-production-please-keep-this-long

app.kafka.topics.user-registered=user-registered

app.auth.login.max-email-attempts=5
app.auth.login.max-ip-attempts=20
app.auth.login.window-seconds=300
app.auth.login.lockout-seconds=900
app.auth.email-verification.ttl-seconds=600
app.auth.email-verification.resend-cooldown-seconds=60
app.auth.email-verification.max-attempts=5
app.auth.password-reset.ttl-seconds=600
app.auth.password-reset.resend-cooldown-seconds=60
app.auth.password-reset.max-attempts=5
app.email.provider=none
app.email.from=no-reply@payflux.local
app.email.enabled=true
